const sharedData = window.AR_SHARED_DATA || {};

const menuData = sharedData.menuData || [];

const restaurantConfig = sharedData.restaurantConfig || {
    name: 'La Palma',
    currency: '$',
    primaryColor: '#8b2d2d',
    secondaryColor: '#2d5a3d'
};
